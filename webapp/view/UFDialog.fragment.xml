<core:FragmentDefinition
   xmlns="sap.m"
   xmlns:core="sap.ui.core">
   <Dialog
		id="UFDialog"
		title="{view>/titulo}"
		class="sapUiSmallMargin sapUiSizeCompact">
   	
   		<VBox id="vBoxUF" class="sapUiSmallMargin">
   			<Label text="Código:" labelFor="codigo"/>
			<Input value="{Codigo}" id="codigo" required="true"/>
			
			<Label text="Sigla:" labelFor="sigla"/>
			<Input value="{Sigla}" id="sigla" required="true"/>
		</VBox>
   		
   		<buttons>
   			<Button text="Salvar" press="onSaveDialog" type="Accept"/>
   			<Button text="Fechar" press="onCloseDialog" type="Reject"/>
   		</buttons>
   </Dialog>
</core:FragmentDefinition>